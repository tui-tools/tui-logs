# GoReleaser configuration for tui-logs, from the family's shared template.
#
# One repository, one binary, one tag series: `git tag v0.1.0 && git push
# origin v0.1.0` is what publishes a release.
version: 2

project_name: tui-logs

before:
  hooks:
    - go mod download

builds:
  - id: tui-logs
    main: ./cmd/tui-logs
    binary: tui-logs
    env:
      # Static binaries: no libc dependency, one file to copy anywhere.
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
    flags:
      - -trimpath
    ldflags:
      - -s -w -X main.version={{ .Version }}

archives:
  - id: tui-logs
    ids:
      - tui-logs
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    formats:
      - tar.gz
    files:
      - LICENSE
      - README.md
      - examples/config.toml

# The distribution packages. Three formats times two architectures is six
# files per release, and they are what pkgs.tui.tools indexes into the apt,
# dnf and pacman repositories: a tool is then installed and upgraded the way
# everything else on the machine is.
#
# GoReleaser cannot read tool.json, so `description` below is a copy of the
# manifest's tagline. tui-kit/tools/check-nfpm.py asserts the two are
# identical, and `make manifest` runs it, so the copy cannot drift.
nfpms:
  - id: tui-logs
    ids:
      - tui-logs
    package_name: tui-logs
    formats:
      - deb
      - rpm
      - archlinux
    # Each ecosystem's own naming, so the files look native where they land:
    # tui-logs_0.1.0_amd64.deb, tui-logs-0.1.0.x86_64.rpm,
    # tui-logs-0.1.0-1-x86_64.pkg.tar.zst.
    file_name_template: "{{ .ConventionalFileName }}"
    vendor: tui-tools
    homepage: https://tui.tools/tools/tui-logs/
    maintainer: tui-tools <maintainers@tui.tools>
    description: "The journal on one screen: narrow it, follow it, and shrink it safely"
    license: MIT
    # The binary belongs in the distribution's own path, not /usr/local: it
    # arrives through the package manager, so the package manager owns it.
    bindir: /usr/bin
    section: utils
    priority: optional
    contents:
      - src: README.md
        dst: /usr/share/doc/tui-logs/README.md
      - src: LICENSE
        dst: /usr/share/doc/tui-logs/LICENSE
      - src: examples/config.toml
        dst: /usr/share/doc/tui-logs/config.toml
    # The package owns the binary name, so anything else shipping the same
    # command — an AUR -bin build, a hand-rolled local package — is a
    # conflict rather than a silent overwrite.
    provides:
      - tui-logs
    conflicts:
      - tui-logs-bin
    rpm:
      group: Applications/System
    archlinux:
      pkgbase: tui-logs
      packager: tui-tools <maintainers@tui.tools>

checksum:
  name_template: checksums.txt

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

release:
  prerelease: auto
